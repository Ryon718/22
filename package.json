{
  "name": "call-bomber-backend",
  "version": "1.0.0",
  "description": "Twilio Voice Handler for Call Bombing App.",
  "main": "api/voice-handler.js",
  "scripts": {
    "start": "node api/voice-handler.js",
    "dev": "nodemon api/voice-handler.js" 
  },
  "keywords": [
    "twilio",
    "bombing",
    "webhook"
  ],
  "author": "Your Name Here",
  "license": "MIT",
  "dependencies": {
    "twilio": "^4.20.0",   // <-- ✅ 修复了：这里加上了逗号
    "dotenv": "^16.3.1"    // <-- ✅ 确保它后面有逗号（如果还有其他依赖）
  },
  "devDependencies": {
    "nodemon": "^3.0.2"     // <-- ✅ 修复了：这里加上了逗号
  }
}
